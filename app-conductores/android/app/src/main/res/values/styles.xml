<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Tema de runtime de la app: SIEMPRE claro, independiente del tema del
         sistema. La interfaz web conserva su apariencia en modo claro y oscuro.
         La franja del status bar se adapta al modo del sistema desde
         MainActivity (color + iconos).
         windowOptOutEdgeToEdgeEnforcement=true restaura el layout clásico
         (contenido debajo de status bar / notch) en Android 15+ con targetSdk 35. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>


    <!-- Tema con el que se CREA la ventana de la actividad (splash). El opt-out
         de edge-to-edge debe declararse AQUÍ: los flags de ventana se deciden
         al crearla con este tema. Declararlo solo en el postSplashScreenTheme
         no tiene efecto. La franja del status bar queda pintada con el color
         de la app para mantener el contraste durante el splash. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
        <item name="android:statusBarColor">#F2F3F4</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="windowSplashScreenBackground">#F2F3F4</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>