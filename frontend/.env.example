# URL del backend API
# En desarrollo: http://localhost:3458
# En produccion: https://tu-dominio.com
VITE_API_URL=http://localhost:3458/api

# Servicio OCR de placas (lectura de placa desde foto del vehiculo).
# Se consume con fetch directo SIN token (servicio interno de la LAN):
# mantenlo aislado de internet o proxéalo por la API principal antes de exponerlo.
# Si no se define, la lectura automatica de placa queda deshabilitada.
# VITE_PLATE_API=http://localhost:8000
